#include <bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    int dem[20001]={0};
    int a[n];
    dem[0]=1;
    int count=0;
    vector<int> m;
    for (int i=0;i<n;i++){
    	cin>>a[i];
	}
	int r,l=0;
	for (r=1;r<=n;r++){
		dem[a[r]]++;
		while (dem[arr[r]])
	}
	return 0;
}
